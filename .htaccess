# Use my custom 404 page  
ErrorDocument 404 /404.php

# Optional: prevent directory listing
Options -Indexes

# --- Force HTTPS ---
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
